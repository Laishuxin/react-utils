{
  "name": "env-form",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "start:server": "json-server __server__/db.json --watch --port=3010",
    "format": "yarn prettier --write .",
    "prepare": "husky install"
  },
  "dependencies": {
    "json-server": "^0.16.3",
    "react": "^17.0.0",
    "react-dom": "^17.0.0"
  },
  "devDependencies": {
    "@commitlint/cli": "^13.1.0",
    "@commitlint/config-conventional": "^13.1.0",
    "@vitejs/plugin-react-refresh": "^1.3.1",
    "husky": "^7.0.1",
    "lint-staged": "^11.1.2",
    "prettier": "2.3.2",
    "vite": "^2.4.4"
  },
  "lint-staged": {
    "*.{js,css,md,jsx,ts,tsx}": "prettier --write"
  }
}
